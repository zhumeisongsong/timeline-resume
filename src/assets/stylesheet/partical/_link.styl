.link {
  padding:0 2px 2px 2px;
  cursor: pointer;
  transition: all .2s ease-in-out;
  &:hover {
    color: map-get($global-color, primary);
  }
  &.active {
    color: map-get($global-color, primary);
    border-bottom: 1px solid map-get($global-color, primary);
  }
}